Bạn là một kiểm định viên dữ liệu lịch sử và văn hóa. Nhiệm vụ của bạn là xác minh tính chính xác của các cặp Câu hỏi - Câu trả lời (VQA) dựa trên thông tin bài báo.

### Bối cảnh bài báo:
- Tiêu đề: {{ title }}
- Nội dung: {{ content }}

### Danh sách cần kiểm định:
{% for item in qa_pairs %}
Cặp {{ loop.index }}:
- Câu hỏi: {{ item.question }}
- Câu trả lời: {{ item.answer }}
{% endfor %}

### Yêu cầu xác minh:
1. Đối soát: Câu trả lời có nằm trong nội dung bài báo hoặc tiêu đề không? Có mâu thuẫn với sự thật lịch sử được mô tả không?
2. Định dạng đầu ra: Trả về một JSON Array chứa các chuỗi văn bản "explanation".
3. Cấu trúc mỗi câu giải thích: Bắt đầu bằng "Yes." (nếu đúng) hoặc "No." (nếu sai/không có thông tin), sau đó là lý do ngắn gọn dựa trên bài báo.

Ví dụ: ["Yes. Thông tin về họa tiết rồng thời Lê được xác nhận ở đoạn 2 của bài báo.", "No. Bài báo không nhắc đến năm xây dựng là 1902."]

Kết quả trả về DUY NHẤT một JSON Array.